# 快速开始

推荐通过根包 Client 接入。它负责后台执行，应用通过 Run 句柄等待结果、取消任务和提交审批。本文对应 v0.1.0，Client 与恢复能力目前为实验性。

## 安装与准备

需要 Go 1.24+。已有 Go module 可跳过前两行：

```bash
mkdir goai-demo
cd goai-demo
go mod init example.com/goai-demo
go get github.com/dingkui/dlz-goai@v0.1.0
```

启动本地 Ollama（未运行时执行 `ollama serve`），在另一个终端准备模型：

```bash
ollama pull qwen3:8b
```

## 第一次运行

保存为 `main.go`，执行 `go run .`，终端将显示模型生成的一句话。这个例子没有工具，不需要审批。

```go
package main

import (
    "context"
    "fmt"
    "log"

    dlzgoai "github.com/dingkui/dlz-goai"
    "github.com/dingkui/dlz-goai/message"
    "github.com/dingkui/dlz-goai/provider/ollama"
)

func main() {
    provider := ollama.New("")
    client, err := dlzgoai.NewClient(dlzgoai.Options{
        Model: func(ctx context.Context, msgs []message.Message, opts *message.Options, emit func(message.Delta)) error {
            return provider.ChatStream(ctx, "qwen3:8b", msgs, opts, emit)
        },
    })
    if err != nil {
        log.Print(err)
        return
    }
    defer client.Close()

    run, err := client.Start(context.Background(), dlzgoai.Request{Input: "用一句话介绍 Go。"})
    if err != nil {
        log.Print(err)
        return
    }
    result, err := run.Wait(context.Background())
    if err != nil {
        log.Print(err)
        return
    }
    fmt.Println(result.Content)
}
```

`Start` 返回的是运行句柄，后台执行的结果与错误通过 `Wait` 获取。默认使用内存存储，进程退出后记录消失。

## 加入业务工具

以下是配置片段，需要导入 `tool` 包。将工具传入 `dlzgoai.Options.Tools`：

```go
lookup := tool.NewFunc("lookup_ticket", "查询工单", nil, true,
    func(ctx context.Context, args map[string]any) (tool.Result, error) {
        return tool.Text("T-1024: 待处理"), nil
    })
// 在 NewClient 的 Options 中配置：Tools: []tool.Tool{lookup}
```

工具调用需要模型支持。真实业务参数建议使用 `tool.Typed` 自动生成 schema，详见 [工具手册](使用手册/tool.md)。写操作应如实声明为非只读，Client 默认通过 Broker 等待审批；显式免审批使用 `Request.Policies`，不要改变只读声明来绕过审批。

## 不依赖模型服务的完整演示

下列命令在库仓库根目录执行，与上面的独立项目二选一：

```bash
git clone https://github.com/dingkui/dlz-goai.git
cd dlz-goai
```

仓库中的 [fullstack 示例](../examples/fullstack/main.go) 演示 Client、模拟审批、取消后续跑与回放。在仓库根目录执行：

```bash
go run ./examples/fullstack
```

这属于同进程模拟中断，不是进程崩溃测试。运行前提和预期输出见 [实战教程](实战案例-接入现有Go项目.md)。

## 下一步

- [Client 手册](使用手册/client.md)：请求、生命周期、事件流和 SQLite 装配。
- [实战教程](实战案例-接入现有Go项目.md)：将工单业务接入 Client。
- [事件与流式集成](指南/事件与流式集成.md)：开始任务、订阅与审批端点。
- [模型手册](使用手册/llm.md)：换用 OpenAI 兼容服务。
- [文档导航](README.md)：按任务选择阅读路径。

## 常见问题

| 现象 | 处理 |
|---|---|
| `model is required` | 给 `NewClient` 提供 `Options.Model`。 |
| 服务端提示不支持 tools | 换用支持工具调用的模型；普通对话使用空工具集。 |
| Wait 一直等待，状态为 waiting_approval | 通过 `GetRun` 查询待审批调用，再由独立界面或端点提交 `Approve`；不能等待结果后才处理审批。 |
| Start 成功但没有结果 | 检查 `Wait` 返回的错误；提交成功不等于模型或存储执行成功。 |
| Wait 超时后任务仍在运行 | 这是等待语义；停止任务需显式 `Cancel`。 |
| 重启后没有历史 | 默认存储在内存；需要记录保留时显式注入 SQLite Runtime。 |
